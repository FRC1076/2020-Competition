language: python
python:
  - "3.8"
# command to install dependencies
install:
  - pip install --no-binary robotpy-rev 
  - pip install -r requirements.txt 
# command to run tests
script:
- py -3 /tests/drivetrain_test.py